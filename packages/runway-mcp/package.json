{
  "name": "@runway-playground/mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "MCP server for Runway media generation (MCP Apps)",
  "main": "dist/index.js",
  "scripts": {
    "build:shared": "tsc -p ../runway-shared/tsconfig.build.json",
    "build": "pnpm run build:shared && mcp-use build",
    "dev": "mcp-use dev",
    "start": "pnpm run build && NODE_ENV=production mcp-use start"
  },
  "dependencies": {
    "@runway-playground/shared": "file:../runway-shared",
    "@runwayml/sdk": "^2.8.2",
    "mcp-use": "1.24.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "react-router": "^7.13.0",
    "zod": "^4.3.5"
  },
  "devDependencies": {
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
