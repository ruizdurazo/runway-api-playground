# Hosted: Dashboard → Settings → API.
# Local: run `npm run supabase:start`, then copy URL + Publishable key from the output or `npm run supabase:status`.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
